{
  "name": "my-web-app",
  "version": "1.0.0",
  "scripts": {
    "lint": "echo 'Linter: No errors found.'",
    "test": "echo 'Tests: All suites passed.'",
    "build": "mkdir -p build && echo '<h1>Build Success</h1>' > build/index.html",
    "start": "npx serve -s build -l 3000"
  },
  "devDependencies": {
    "serve": "^14.0.0",
    "wait-on": "^7.0.0"
  }
}
