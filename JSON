{
  "name": "my-web-app",
  "version": "1.0.0",
  "description": "Scenario 1: Web Application with CI/CD Performance Gates",
  "main": "index.js",
  "scripts": {
    "lint": "echo 'Linter: No errors found.' && exit 0",
    "test": "echo 'Tests: All suites passed.' && exit 0",
    "build": "mkdir -p build && echo '<h1>Build Success</h1>' > build/index.html && exit 0",
    "start": "npx serve -s build -l 3000"
  },
  "devDependencies": {
    "serve": "^14.0.0",
    "wait-on": "^7.0.0"
  }
}
